# Production environment — used by Vite when building for deployment.
# Relative /api routes requests to the Vercel serverless function on the same origin.
# Override VITE_API_URL in the Vercel dashboard if the backend lives on a separate host.
VITE_API_URL=/api
VITE_SOCKET_URL=
